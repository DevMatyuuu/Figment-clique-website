generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model catalog {
  id         String    @id @default(auto()) @map("_id") @db.ObjectId
  title      String    @unique
  price      Int
  image      String
  image2     String
  featured   Boolean   @default(false) 
  category   String
}

model stocks {
  id         String    @id @default(auto()) @map("_id") @db.ObjectId
  catalogTitle String  @unique
  small      Int
  medium     Int
  large      Int
  xl         Int
  xxl        Int
}


